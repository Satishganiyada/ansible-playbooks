---
- name: install and start nginx
  hosts: myservers
  become: true

  tasks:
    - name: install nginx
      apt:
        name: nginx
        state: present
        Update_cache: yes
    - name: start service
      service:
        name: nginx
        state: started
        become: true      

